import { Component } from '@angular/core'

/**
 * Main app component
 * Base component to load via 'router-outlet' all other pages
 */
@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.scss'],
})
export class AppComponent { }
